import { ProcessedDatabucket } from '@mockoon/commons';
import { Request, Response } from 'express';
export type CrudRouteIds = 'get' | 'getbyId' | 'create' | 'update' | 'updateById' | 'updateMerge' | 'updateMergeById' | 'delete' | 'deleteById';
type CrudRoutes = {
    id: CrudRouteIds;
    method: string;
    path: string;
}[];
export declare const crudRouteParamName = "id";
/**
 * Creates a set of CRUD routes for a given route path
 *
 * @param routePath
 * @returns
 */
export declare const crudRoutesBuilder: (routePath: string) => CrudRoutes;
/**
 * Creates a set of CRUD actions for a given databucket type
 */
export declare const databucketActions: (crudId: CrudRouteIds, databucket: ProcessedDatabucket, request: Request, response: Response) => any;
export {};
