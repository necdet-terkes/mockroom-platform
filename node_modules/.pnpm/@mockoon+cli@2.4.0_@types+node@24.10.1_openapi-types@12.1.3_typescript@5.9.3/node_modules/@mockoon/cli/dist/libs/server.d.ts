import { FileTransportOptions } from 'winston/lib/winston/transports';
export declare const createServer: (parameters: {
    data: string;
    environmentDir: string;
    logTransaction?: boolean;
    fileTransportsOptions?: FileTransportOptions[];
}) => void;
